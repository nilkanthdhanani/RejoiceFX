// import React, { memo } from 'react'
// import { ReactLazyLoad } from 'react-lazyload'


// const LazyLoad = ({ children, offset = 0, height = 10, id, once = true }) => {
//   return (
//     <ReactLazyLoad
//       offset={offset}
//       height={height}
//       once={once}
//       {...(id
//         ? {
//           placeholder: <div id={id} style={{ height: height }} />,
//         }
//         : {})}
//     >
//       {children}
//     </ReactLazyLoad>
//   )
// }

// export default memo(LazyLoad)
